# -*- mode: python -*-
a = Analysis(['s_enum2.py'],
             pathex=['/home/zkhan/source/mchlres/ndirfil/mdir/zzzz'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='s_enum2',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='s_enum2')
